# 🔐 إعداد التوقيع التلقائي في GitHub Actions

## ✅ ما تم تطبيقه:

1. **تحديث Workflow** لاستخدام `macos-15` (أحدث Xcode 16.4)
2. **دعم التوقيع التلقائي** باستخدام App Store Connect API
3. **Fallback تلقائي** إلى Manual Signing إذا لم يتم توفير API Key

---

## 📋 الخطوات المطلوبة

### 1️⃣ **إضافة Secrets إلى GitHub Repository**

انتقل إلى:
```
Repository Settings → Secrets and variables → Actions → New repository secret
```

أضف الـ Secrets التالية:

#### **Secret 1: APPSTORE_API_KEY_BASE64**
قم بتحويل ملف `.p8` إلى base64:

```bash
cd /home/ameen/Desktop/client-app/cer_ios
base64 -w 0 "AuthKey_49B2H4YJHU (1).p8"
```

انسخ الناتج وأضفه كـ Secret باسم: `APPSTORE_API_KEY_BASE64`

#### **Secret 2: APPSTORE_API_KEY_ID**
القيمة: `49B2H4YJHU`

#### **Secret 3: APPSTORE_API_ISSUER_ID**
القيمة: `0a92da91-c697-4b47-ab81-4ff8185080fa`

---

### 2️⃣ **التحقق من صلاحيات API Key**

تأكد من أن API Key لديه الصلاحيات التالية في App Store Connect:

- ✅ **Access To**: App Manager أو Admin
- ✅ **Certificates, Identifiers & Profiles**: قراءة وكتابة

---

## 🎯 الفوائد

### ✅ **مع App Store Connect API (Automatic Signing):**
- 🔄 **التوقيع التلقائي**: Xcode يختار الشهادة والـ profile تلقائياً
- 📱 **لا حاجة لتحديد UUID**: يعمل على جميع الأجهزة المسجلة
- 🔐 **آمن**: لا حاجة لتضمين الشهادات يدوياً
- 🚀 **سهل الصيانة**: لا حاجة لتحديث الشهادات يدوياً

### ⚙️ **بدون API Key (Manual Signing - Fallback):**
- 🔧 **يعمل حالياً**: باستخدام الملفات الموجودة في `cer_ios/`
- 📱 **قابل للتثبيت**: على جميع الأجهزة في provisioning profile
- ⚠️ **يحتاج صيانة**: عند انتهاء صلاحية الشهادات

---

## 🔍 معلومات إضافية

### **UUID الهاتف المتصل حالياً:**
```
00008030-001A755C2ED2402E
```

### **نوع Provisioning Profile الحالي:**
- Type: **Ad-Hoc**
- Method: سيستخدم `ad-hoc` تلقائياً في الـ workflow
- يعمل على جميع الأجهزة المسجلة في الـ profile

---

## 🚀 كيفية تشغيل البناء

### **الطريقة 1: تلقائياً عند Push**
```bash
git add .
git commit -m "Enable automatic signing"
git push origin main
```

### **الطريقة 2: يدوياً (Manual Trigger)**
1. اذهب إلى **Actions** في GitHub
2. اختر **iOS (macOS)** workflow
3. اضغط **Run workflow**
4. (اختياري) فعّل SSH debug للـ troubleshooting

---

## 📝 ملاحظات مهمة

1. **لا حاجة لتحديد UUID محدد** - التطبيق سيعمل على جميع الأجهزة المسجلة
2. **Xcode 16.4** هو الإصدار المستخدم في `macos-15` runner
3. إذا لم تضف الـ API Key Secrets، سيعمل الـ workflow بـ **Manual Signing** تلقائياً
4. يمكنك حذف خطوة **"Setup iOS code signing"** القديمة إذا أردت تبسيط الكود أكثر

---

## 🆘 استكشاف الأخطاء

### **إذا فشل البناء:**

1. **تحقق من Secrets:**
   ```
   Settings → Secrets → Actions
   ```
   تأكد من وجود الـ 3 secrets

2. **تحقق من صلاحيات API Key:**
   - App Store Connect → Users and Access → Keys
   - تأكد من Active status

3. **تفعيل SSH Debug:**
   - Run workflow manually
   - Enable "SSH debug session"
   - ستحصل على terminal للـ debugging المباشر

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من logs في GitHub Actions
2. ابحث عن السطر الذي يحتوي على `❌` أو `error`
3. راجع قسم "استكشاف الأخطاء" أعلاه

---

**تم إعداده بتاريخ:** 15 يناير 2026
**Xcode Version:** 16.4 (على macos-15 runner)
**Flutter:** Stable channel
